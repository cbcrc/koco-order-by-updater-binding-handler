{
  "name": "koco-order-by-updater-binding-handler",
  "version": "1.2.0",
  "homepage": "https://github.com/cbcrc/koco-order-by-updater-binding-handler",
  "authors": [
    "Mark Moyes <mark.moyes@cbc.ca>"
  ],
  "description": "koco-order-by-updater-binding-handler",
  "main": "src/koco-order-by-updater-binding-handler.js",
  "keywords": [
    "koco",
    "knockout",
    "lists"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "dependencies": {
    "koco": "^1.0.0",
    "koco-string-utilities": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
